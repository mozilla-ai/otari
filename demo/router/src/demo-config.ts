// Demo seed gate, shared by both walkthroughs so they agree. One example is one
// record, so this is in examples; low so a few submissions warm a partition on
// screen. The real gateway default is OTARI_ROUTER_SEED_COUNT=20.
export const SEED_COUNT = 3;
