// Demo seed gate, shared by both walkthroughs so they agree. Low so a couple of
// submissions warm a partition on screen; the real gateway default is
// OTARI_ROUTER_SEED_COUNT=20.
export const SEED_COUNT = 8;
