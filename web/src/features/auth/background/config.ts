export interface LoginBackgroundConfig {
  columns: number
  speed: number
  intensity: number
  contrast: number
  spacing: number
  rounding: number
  waveScale: number
}

export const BAR_ROW_RATIO = 228.6 / 9 / (657.8 / 55)
